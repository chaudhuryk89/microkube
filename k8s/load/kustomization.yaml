apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: loadtest
resources:
  - cpu-burn.yaml
  - cpu-spike-cronjob.yaml
  # memory-spike-job.yaml — apply manually: kubectl apply -f k8s/load/memory-spike-job.yaml
  # http-load-job.yaml — apply manually: kubectl apply -f k8s/load/http-load-job.yaml
